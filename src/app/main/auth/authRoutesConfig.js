import ForgotPasswordPageConfig from './ForgotPasswordPageConfig';
import LoginConfig from './LoginConfig';
import RegisterPageConfig from './RegisterPageConfig';

const authRoutesConfig = [RegisterPageConfig, LoginConfig, ForgotPasswordPageConfig];
export default authRoutesConfig;
